# Story 1.1: Turborepo 모노레포 초기 설정

## 메타 정보
- **Epic**: Epic 1 - 인프라 및 프로젝트 설정
- **Priority**: High
- **Estimate**: 1.5일 (12시간)
- **Dependencies**: 없음

---

## Description

Turborepo + pnpm workspace를 사용한 모노레포 프로젝트 초기 설정

### 작업 내용
- Turborepo + pnpm workspace 설정
- apps/web, apps/api, packages/shared 구조 생성
- TypeScript 공통 설정 (tsconfig)
- ESLint + Prettier 설정

---

## Acceptance Criteria

- [x] `pnpm install` 정상 실행
- [x] `turbo build` 정상 실행
- [x] 3개 패키지(web, api, shared) 간 import 가능 (workspace 구조 설정 완료)
- [x] ESLint 룰 통과

---

## Implementation Notes

### Mitigation
- Vercel의 공식 Turborepo 템플릿(`npx create-turbo@latest`) 사용
- 빈 프로젝트에서 시작하는 대신 템플릿 기반 커스터마이징
- 예상 시간: 12시간 → 6시간으로 단축 가능

### 폴더 구조
```
my-blog/
├── apps/
│   ├── web/          # React + Vite
│   └── api/          # NestJS
├── packages/
│   └── shared/       # 공통 타입, 유틸
├── package.json
├── pnpm-workspace.yaml
└── turbo.json
```

---

## References
- [Turborepo 공식 문서](https://turbo.build/repo/docs)
- [pnpm workspace](https://pnpm.io/workspaces)

---

**작성일**: 2025-11-14
